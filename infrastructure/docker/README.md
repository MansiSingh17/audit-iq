# Docker Scripts

Build and deployment scripts for Docker containers.

## Scripts
- `build-all.sh` - Build all images
- `push-all.sh` - Push to registry
- `clean.sh` - Clean resources
- `docker-compose.dev.yml` - Dev environment

## Usage
```bash
./build-all.sh
./push-all.sh docker.io
```
